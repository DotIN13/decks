{
	"name": "@decks/web-gate",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"description": "A DevTools Protocol proxy that lets a browser agent drive a real Chrome, holding two things it may not do alone: sending anything, and choosing its own words.",
	"exports": {
		".": "./src/index.ts"
	},
	"scripts": {
		"typecheck": "tsc -p tsconfig.json --noEmit",
		"test": "node --import tsx --test \"src/**/*.test.ts\""
	},
	"devDependencies": {
		"@types/node": "^22.19.0",
		"@types/ws": "^8.5.13",
		"tsx": "^4.20.7",
		"typescript": "^5.9.3"
	},
	"dependencies": {
		"ws": "^8.18.0"
	}
}
